api:
  dashboard: true

log:
  level: DEBUG

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    network: traefik

  # This allows Traefik to serve custom local certificates
  file:
    directory: /custom-config
